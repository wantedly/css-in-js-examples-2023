import { css } from "@emotion/react";
import type {} from "@emotion/react/types/css-prop";

export default () => {
  return <button
    css={css({
      background: 'red',
    })}
  >Test</button>;
};
